//function that converts values from [deg/s] to [rad/s]
function unitConverter() {
  var item = sel.value();
  
  textSize(15);
  

  // INSTRUCTIONS BELOW ******************************************
  /*
  STEP 6: 
  Using conditional statements, write the code for converting values of the variables: 
      i. per (period)
      ii. freq (frequency)
  iii. angVel (angular velocity)

  to appropriate units:
  deg/s (degrees per second) 
  rad/s (radians per second), assuming 1 radian ≈ 57.2958 degrees.

  */
    
}
